const authReducer = (state = {}, action) => {
  // switch (action.type) {
  //   // case value:

  //   //     break;

  //   default:
  //     return state;
  // }
  return state;
};

export default authReducer;
